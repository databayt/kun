import { MediaPanel } from "@/components/root/social/stage-panels";

/**
 * The media stage. A StageNote for now — the showroom (generated assets +
 * kept references) replaces it in the next change.
 */
export default function SocialMediaPage() {
  return <MediaPanel />;
}
