{
  "$comment": "Single source of truth for kun engine metadata. Prose/docs reference this file instead of hardcoding numbers. /health (.claude/scripts/health.sh) reads it and warns when docs or reality drift from these values. Agent frontmatter uses a model alias (`opus`/`sonnet`/`haiku` per `model_tiers`) — never pin a version number there.",
  "model": "claude-fable-5",
  "model_label": "Fable 5",
  "fallback_models": ["claude-opus-4-8", "claude-sonnet-5"],
  "engine_version": "databayt v4.0",
  "updated": "2026-07-10",
  "min_claude_code_version": "2.1.190",
  "billing": {
    "plan": "claude-max-100",
    "$comment": "Max 5x ($100/mo) for the time being — corrected from Max 20x on 2026-06-12. Subscription-ONLY posture: no usage-based credits, no API-key per-token spend — changing this requires a /decide entry + Abdout approval (captain ESCALATE gate: budget > $50/mo). Check /usage per-category weekly to stay inside plan caps."
  },
  "sync": {
    "$comment": "Self-update stamps per tier — /sync refreshes them; the captain flags overdue tiers at session start. null = never synced (due immediately). Sources of the form gh:owner/repo are read via `gh api .../releases`. Material adoptions land in docs/CONFIG-BENCHMARK.md → Adoption log.",
    "anthropic": "2026-07-10",
    "stack": "2026-07-10",
    "services": "2026-07-10",
    "practice": "2026-07-10",
    "claude_code_version": "2.1.206",
    "cadence_days": {
      "anthropic": 7,
      "stack": 14,
      "services": 14,
      "practice": 30
    },
    "sources": {
      "anthropic": [
        "https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md",
        "https://platform.claude.com/docs/en/release-notes/api",
        "https://www.anthropic.com/news"
      ],
      "stack": [
        "https://nextjs.org/blog",
        "https://react.dev/blog",
        "https://devblogs.microsoft.com/typescript/",
        "https://tailwindcss.com/blog",
        "gh:prisma/prisma",
        "https://ui.shadcn.com/docs/changelog"
      ],
      "services": [
        "https://vercel.com/changelog",
        "https://neon.com/docs/changelog",
        "https://github.blog/changelog/",
        "https://www.figma.com/release-notes/"
      ],
      "practice": [
        "https://www.anthropic.com/engineering",
        "https://agents.md",
        "gh:bmad-code-org/BMAD-METHOD",
        "community Claude Code configs — survey set in docs/CONFIG-BENCHMARK.md → Sources"
      ]
    }
  },
  "counts": {
    "project_agents": 19,
    "user_agents": 46,
    "commands": 0,
    "project_skills": 49,
    "user_skills": 66,
    "pattern_cards": 11,
    "project_rules": 5,
    "domain_rules": 37,
    "project_mcp": 25,
    "user_mcp": 25,
    "hooks": 9,
    "vocabulary_spells": 177,
    "workflows": 4,
    "$plugins_comment": "The two plugins are how the fleet reaches other machines. kun-company also carries the rules, pattern cards, hooks, and workflows. Union of both skill sets is 59 (build ships in both).",
    "plugin_kun_company_agents": 19,
    "plugin_kun_company_skills": 45,
    "plugin_kun_stack_agents": 27,
    "plugin_kun_stack_skills": 15
  },
  "eval": {
    "$comment": "Declared POLICY for the bench layers. Measured state lives in .claude/memory/skill-scores.json — never here, because every engine.json field is something health.sh checks reality against and a score has no declared counterpart. listing_cap is a shared, zero-sum budget: the fleet listing is loaded into EVERY session before a word is typed, so accuracy bought with prefix bloat is a regression, not a win. Raising a cap is a deliberate, recorded act — never a silent accommodation of the thing that just breached it.",
    "listing_cap": 26500,
    "skill_cap": 1200,
    "holdout_pct": 20,
    "cadence_days": 7,
    "listing_cap_history": [
      {
        "date": "2026-08-06",
        "from": 26000,
        "to": 26500,
        "why": "absorbed two deliberate additions made before any baseline existed: the deploy frontmatter fix (+640, a proven dispatch defect) and the bench skill itself (+596). Trimming draft/higgs prose would also have fit under 26000, but tuning descriptions before the first measurement is exactly what this system exists to stop. The next reset requires evidence from a scored run."
      }
    ]
  },
  "model_tiers": {
    "$comment": "Per-agent model alias by task type. The portable stack fleet (~/.claude/agents) follows this; the project fleet defaults to opus (quality-over-speed) except purely mechanical agents. Every agent also carries `effort:` frontmatter per delegation.efforts (added 2026-07-26).",
    "fable": "the main loop — interactive sessions and captain-grade judgment (session default since 2026-07-10); agents stay on the alias tiers below",
    "opus": "architecture, orchestration, deep reasoning, code review/fixes, and strategy/leadership agents (incl. captain)",
    "sonnet": "standard build agents + the mechanical project agent (package)",
    "haiku": "routine/formatting agents (comment, git, icon, github, semantic, sse, structure)"
  },
  "delegation": {
    "$comment": "Token-economy routing — three axes: model tier × effort × product lane. Playbook: docs/TOKEN-ECONOMY.md; on-demand audit: /economy. Facts that shape it: Fable/Mythos tokenizer counts ~30% more tokens than pre-4.7 models for the same text; Fable cannot disable extended thinking (effort is the lever); prompt-cache TTL is 1h on subscription; opus-class burns ~5x sonnet against plan windows.",
    "efforts": {
      "low": "haiku agents — mechanical/formatting work (comment, git, icon, github, semantic, sse, structure); workflow stages that transform rather than reason",
      "medium": "sonnet build agents — standard implementation with known patterns",
      "high": "opus specialists — default ceiling for agents; review, fixes, product references, strategy",
      "xhigh": "judgment tier ONLY — captain, architecture, orchestration; main-loop deep design on explicit request",
      "policy": "every agent carries `effort:` frontmatter matching its model tier; Workflow scripts pass opts.effort per stage (low for mechanical stages, high+ only for verify/judge)"
    },
    "products": {
      "$comment": "All Anthropic surfaces draw the SAME Max pool (5h session window + weekly window, shared with chat and Cowork). Product delegation optimizes tokens-per-outcome, not separate quotas — only the a/h lanes are off-pool.",
      "claude-code": "build work only — heaviest per-turn context (rules + MCP + repo files travel with every message)",
      "cowork": "planning, research, decisions, docs — no repo/MCP preamble; handoff via ~/.claude/bridge.md",
      "claude-chat": "pure Q&A, drafting, brainstorming — cheapest surface, zero engine preamble; use before opening a Code session for think-work",
      "claude-web-code": "long-running cloud babysitting (autofix-pr, scheduled routines) — same pool, so keep routines lean and infrequent",
      "antigravity": "OFF-POOL cheap lane (`a` = Gemini) — easy one-file mechanical tasks; zero Anthropic tokens",
      "hermes": "OFF-POOL chat relay (`h` = Nous) — gateway/channel tasks, never build work"
    },
    "session_hygiene": "/clear between unrelated tasks (stale context taxes every message); /compact with focus instructions on long tasks; batch touches inside the 1h cache window — a >1h idle gap reprocesses full context; plan mode before big builds; specific prompts over broad scans"
  },
  "commit_footer": "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
}
